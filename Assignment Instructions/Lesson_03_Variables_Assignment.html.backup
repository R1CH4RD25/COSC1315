<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lesson 03: Variables - Assignment Instructions</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            background-color: #f4f4f4;
        }
        .container {
            background-color: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        h1 {
            color: #2c3e50;
            border-bottom: 3px solid #3498db;
            padding-bottom: 10px;
        }
        h2 {
            color: #2980b9;
            margin-top: 25px;
        }
        ul, ol {
            margin: 15px 0;
            padding-left: 30px;
        }
        li {
            margin: 10px 0;
        }
        li p {
            margin: 5px 0;
        }
        code {
            background-color: #f8f9fa;
            padding: 2px 6px;
            border-radius: 3px;
            font-family: 'Courier New', monospace;
            color: #c7254e;
        }
        strong {
            color: #2c3e50;
        }
        .note {
            background-color: #fff3cd;
            padding: 15px;
            margin: 20px 0;
            border-left: 4px solid #ffc107;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Lesson 03 — Variables</h1>

        <h2><strong>Assignment Instructions</strong></h2>
        <p>For this activity, you will:</p>
        <ul>
            <li>
                <p>Open a new notebook in <strong>Google Colab</strong> and give it a title that reflects <strong>Lesson 03 — Variables</strong>.</p>
            </li>
            <li>
                <p>Create variables to store information about your favorite athlete (e.g., <code>player_name = "LeBron James"</code>, <code>jersey_number = 23</code>, <code>points_per_game = 27.5</code>).</p>
            </li>
            <li>
                <p>Use descriptive variable names that follow Python naming conventions (lowercase with underscores, no spaces, must start with a letter).</p>
            </li>
            <li>
                <p>Practice assigning different data types to variables: strings (text), integers (whole numbers), and floats (decimal numbers).</p>
            </li>
            <li>
                <p>Update variable values by reassigning them (e.g., <code>points = 25</code> then <code>points = 30</code>) and print the results to see how variables can change.</p>
            </li>
            <li>
                <p>Create a simple calculation using variables (e.g., calculate total points from field goals and free throws) and store the result in a new variable.</p>
            </li>
            <li>
                <p>Use multiple variables in a single <code>print()</code> statement to display a formatted sentence about your athlete's stats.</p>
            </li>
            <li>
                <p>Save and submit your completed notebook through <strong>Google Classroom</strong> when finished.</p>
            </li>
        </ul>

        <div class="note">
            <strong>💡 Variable Naming Rules:</strong>
            <ul style="margin: 10px 0;">
                <li>✅ <code>player_name</code>, <code>team_score</code>, <code>average_points</code></li>
                <li>❌ <code>player name</code> (no spaces), <code>2player</code> (can't start with number)</li>
                <li>❌ <code>print</code>, <code>if</code>, <code>for</code> (don't use Python keywords)</li>
                <li>Use meaningful names: <code>jersey_number</code> not <code>x</code></li>
            </ul>
        </div>

        <div class="note">
            <strong>🏀 Example Variables:</strong>
            <ul style="margin: 10px 0;">
                <li><code>team_name = "Lakers"</code> (string)</li>
                <li><code>wins = 45</code> (integer)</li>
                <li><code>win_percentage = 0.732</code> (float)</li>
                <li><code>is_playoff_bound = True</code> (boolean)</li>
            </ul>
        </div>
    </div>
</body>
</html>
